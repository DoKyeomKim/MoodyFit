<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mf.mapper.PostingMapper">
    <select id="getAllPostings">
SELECT 
    P.posting_idx, 
    P.title, 
    P.update_date, 
    S.store_name AS store_name, 
    PR.name AS product_name, 
    PR.price
FROM 
    POSTING P
JOIN 
    PRODUCT_INFO PI ON P.PRODUCT_INFO_IDX = PI.PRODUCT_INFO_IDX
JOIN 
    PRODUCT PR ON PI.PRODUCT_IDX = PR.PRODUCT_IDX
JOIN 
    STORE S ON PR.STORE_IDX = S.STORE_IDX
    </select>
    
   
</mapper>